# results/

Empty in git by design. Every script under `scripts/` creates its own
subdirectory here and never overwrites an existing file.
[`../docs/PAPER_MAP.md`](../docs/PAPER_MAP.md) maps each manuscript table and
figure to the script that writes it and the artefact it lands in.
